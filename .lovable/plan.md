Add a **Case Studies** link to the header navbar (desktop + mobile) in `src/components/india/IndiaNav.tsx`, placed right between **Our Clients** and **Blogs** — same style as Blog link, pointing to `/case-studies`.

Reply **Go** to apply.